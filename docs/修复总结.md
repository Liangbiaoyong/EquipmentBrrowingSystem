# 修复总结

根据《建筑学院仪器共享平台修改3.md》的甲方反馈逐项跟进，按原文档章节编号记录。  
`[x]` 表示已完成，`[ ]` 表示未完成/待验证。

## 一、与角色无关的部分

1. **设备浏览：设备图片放大后加载失败**
   - [x] 改进：点击放大预览统一改为 `/api/v1/files/**` 文件代理地址，修复“加载失败”。

2. **消息中心：以前消息隔几天消失，180天保留未生效**
   - [x] 改进：定时清理改为按“已读/未读”分别清理；已读默认保留 180 天，未读默认永久保留。

## 二、学生账号

1. **取消仪表盘、数据统计、导出CSV**
   - [x] 改进：学生权限移除 `dashboard:view`、`statistics:view`；设备列表对学生隐藏“导出CSV”按钮。

2. **借用申请：同一设备不同人/同一人可重复提交**
   - [x] 改进：时间冲突时直接拒绝整单申请并提示“设备已被占用”，不再静默跳过。

3. **我的借用**
   - 3.1 逾期未显示、已逾期统计为 0
     - [x] 改进：借用概览按记录口径统计，逾期包含 `OVERDUE` 和 `BORROWING` 已到期未还。
   - 3.2 `RETURN_PENDING` 显示英文
     - [x] 改进：前端统一显示为“归还中”，导出报表状态也中文化。
   - 3.3 取走照片可被“更换”
     - [x] 改进：已上传取走照片后不再显示“更换照片”入口。
   - 3.4 借用进度看不到提交归还申请时间
     - [x] 改进：新增 `return_request_time` 字段，并在归还时间线中展示“提交归还申请”时间。

4. **消息中心：通知内容缺少单号/设备信息**
   - [x] 改进：借用申请、审批结果、归还提醒、逾期警告等通知内容增加借用单号。

## 三、教师账号

1. **设备浏览：导出CSV后文件没有内容**
   - [x] 改进：允许有 `device:manage` 的教师导出，并补充借还状态、设备状态、借用类型等列。

2. **设备管理：教师可编辑全学院设备**
   - [x] 改进：教师只能查看/管理自己名下设备；只能修改存放地、借还状态、设备状态、借用类型、描述。
   - [x] 改进：教师不能修改设备名称/型号/删除设备/修改默认审批人。
   - [x] 改进：前端隐藏教师的批量导入、分类管理、实验室管理入口，并新增 `device:import`、`category:manage` 权限。

3. **我的借用**
   - 同步学生账号相关修复。

4. **借用管理**
   - 4.1 点击单号跳到设备详情
     - [x] 改进：借用浏览行点击改为进入借用详情 `/borrows/{id}`。
   - 4.2 教师看到全院借用记录
     - [x] 改进：教师借用浏览/导出/逾期列表按自己名下设备过滤。
   - 4.3 教师没有归还审批权限
     - [x] 改进：教师增加 `return:manage`，且只能看到自己名下设备的归还审批。
   - 4.4 借用详情看不到实际审批人/审批时间/意见
     - [x] 改进：借用详情展示实际初审人/终审人姓名、审批时间和审批意见。
   - 4.5 导出报表为空
     - [ ] 待完善：本轮已增强状态中文化和部分列，仍需补充更多导出列与业务字段。

5. **数据统计**
   - [x] 改进：逾期/借用中统计口径已修正。
   - [ ] 待处理：历史月份趋势、热门设备/高频用户/分类利用率无数据、趋势图渲染异常。

## 四、实验室管理员

1. **设备管理**
   - 1.1 导出CSV与页面内容不一致
     - [x] 改进：导出设备 CSV 增加设备ID、借还状态、设备状态、借用类型、可借数量等列。
   - 1.2 缺少图片列表显示不全
     - [x] 改进：后端改为分页，前端增加搜索框和分页。
   - 1.3 归还照片加载失败
     - [x] 改进：归还审批照片预览改为 `/api/v1/files/**` 代理地址。
   - 1.4 分类管理界面体验
     - [x] 改进：分类列表已支持按ID/名称/排序排序，映射规则按优先级排序。
   - [ ] 待处理：后续可继续按实验室管理模式进一步优化交互。

2. **借用管理**
   - 2.1 终审看不到实际初审人/终审人、审批时间、审批意见
     - [x] 改进：借用详情审批记录已展示实际审批人姓名、时间、意见。
   - 2.2 逾期管理检测不到逾期
     - [x] 改进：统计口径已修正，仍需验证“逾期管理”界面数据一致性。
   - 2.3 归还照片总提示加载失败
     - [x] 改进：已修复预览路径。
   - 2.4 “归还”与“强制归还”功能区分
     - [ ] 待处理：需进一步明确保留/合并/取消其中一项。

3. **数据统计和消息中心**
   - 同教师/学生相关修复。

## 五、系统管理员

1. **设备浏览/设备管理导出缺列**
   - [x] 改进：导出 CSV 已补充页面主要状态列。

2. **默认审批人列表过长、无法恢复默认**
   - [x] 改进：候选人限定为设备使用人或实验室管理员；教师不能修改默认审批人。
   - [x] 改进：新增“恢复默认”操作，可清除已指定的默认审批人，恢复为使用人审批。

3. **系统管理：操作日志每10秒一个error**
   - [x] 核查：当前 sys_log 中未发现周期性“每10秒error”，近期失败操作均为人工操作，暂未复现该问题。

## 全局文案

- [x] 主页/浏览器标题中的“设备借用系统”统一改为“仪器共享平台”。

## 部署状态

- [x] 已完成远程 Docker 构建部署，前端 HTTP 200、后端 API 403（未认证正常）、5 个容器运行正常。
- [x] 远程 GitHub 网络不稳定时，已通过 scp 同步最新文件并重新构建部署。

## 导入导出格式

- [x] 设备列表/设备管理导出同时支持 CSV 和 Excel（XLSX）。
- [x] 设备导入模板、用户导入模板、借用报表、统计报表、数据表导出均同时支持 CSV / XLSX。
